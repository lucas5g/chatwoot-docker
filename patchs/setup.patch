diff --git a/.env.example b/.env.example
index d5c7a76f9..7c45a8d74 100644
--- a/.env.example
+++ b/.env.example
@@ -65,7 +65,7 @@ REDIS_SENTINEL_MASTER_NAME=
 # POSTGRES_DATABASE=
 POSTGRES_HOST=postgres
 POSTGRES_USERNAME=postgres
-POSTGRES_PASSWORD=
+POSTGRES_PASSWORD=postgres
 RAILS_ENV=development
 # Changes the Postgres query timeout limit. The default is 14 seconds. Modify only when required.
 # POSTGRES_STATEMENT_TIMEOUT=14s
diff --git a/Gemfile.lock b/Gemfile.lock
index 0a669b606..341f3b58f 100644
--- a/Gemfile.lock
+++ b/Gemfile.lock
@@ -180,8 +180,8 @@ GEM
       uniform_notifier (~> 1.11)
     bundle-audit (0.1.0)
       bundler-audit
-    bundler-audit (0.9.1)
-      bundler (>= 1.2.0, < 3)
+    bundler-audit (0.9.3)
+      bundler (>= 1.2.0)
       thor (~> 1.0)
     byebug (11.1.3)
     childprocess (5.1.0)
@@ -830,7 +830,6 @@ GEM
       faraday-net_http (>= 1)
       faraday-retry (>= 1)
       marcel (~> 1.0)
-      ruby_llm-schema (~> 0.2.1)
       zeitwerk (~> 2)
     ruby_llm-schema (0.2.5)
     ruby_parser (3.20.0)
@@ -1162,7 +1161,7 @@ DEPENDENCIES
   working_hours
 
 RUBY VERSION
-   ruby 3.4.4p34
+  ruby 3.4.4p34
 
 BUNDLED WITH
-   2.5.16
+  4.0.1
diff --git a/app/javascript/dashboard/routes/dashboard/settings/account/components/BuildInfo.vue b/app/javascript/dashboard/routes/dashboard/settings/account/components/BuildInfo.vue
index 05f206136..54350d2ef 100644
--- a/app/javascript/dashboard/routes/dashboard/settings/account/components/BuildInfo.vue
+++ b/app/javascript/dashboard/routes/dashboard/settings/account/components/BuildInfo.vue
@@ -51,6 +51,7 @@ const copyGitSha = () => {
       >
         {{ `Build ${gitSha}` }}
       </span>
+      ficou bom 123
     </div>
   </div>
 </template>
diff --git a/docker-compose.yaml b/docker-compose.yaml
index facac830f..eaf822e19 100644
--- a/docker-compose.yaml
+++ b/docker-compose.yaml
@@ -1,4 +1,4 @@
-version: '3'
+# version: '3'
 
 services:
   base: &base
@@ -37,7 +37,7 @@ services:
       - 3000:3000
     env_file: .env
     environment:
-      - VITE_DEV_SERVER_HOST=vite
+      - VITE_RUBY_HOST=vite
       - NODE_ENV=development
       - RAILS_ENV=development
     entrypoint: docker/entrypoints/rails.sh
@@ -76,7 +76,7 @@ services:
     ports:
       - "3036:3036" # Vite dev server
     environment:
-      - VITE_DEV_SERVER_HOST=0.0.0.0
+      - VITE_RUBY_HOST=0.0.0.0
       - NODE_ENV=development
       - RAILS_ENV=development
     entrypoint: docker/entrypoints/vite.sh
@@ -88,11 +88,11 @@ services:
     ports:
       - '5432:5432'
     volumes:
-      - postgres:/data/postgres
+      - postgres:/var/lib/postgresql/data
     environment:
       - POSTGRES_DB=chatwoot
       - POSTGRES_USER=postgres
-      - POSTGRES_PASSWORD=
+      - POSTGRES_PASSWORD=postgres
 
   redis:
     image: redis:alpine
diff --git a/docker/Dockerfile b/docker/Dockerfile
index 2d753337d..4da63a42c 100644
--- a/docker/Dockerfile
+++ b/docker/Dockerfile
@@ -11,7 +11,7 @@ ENV PNPM_VERSION=${PNPM_VERSION}
 # For development docker-compose file overrides ARGS
 ARG BUNDLE_WITHOUT="development:test"
 ENV BUNDLE_WITHOUT ${BUNDLE_WITHOUT}
-ENV BUNDLER_VERSION=2.5.11
+ENV BUNDLER_VERSION=4.0.1
 
 ARG RAILS_SERVE_STATIC_FILES=true
 ENV RAILS_SERVE_STATIC_FILES ${RAILS_SERVE_STATIC_FILES}
@@ -105,7 +105,7 @@ ENV PNPM_VERSION=${PNPM_VERSION}
 
 ARG BUNDLE_WITHOUT="development:test"
 ENV BUNDLE_WITHOUT ${BUNDLE_WITHOUT}
-ENV BUNDLER_VERSION=2.5.11
+ENV BUNDLER_VERSION=4.0.1
 
 ARG EXECJS_RUNTIME="Disabled"
 ENV EXECJS_RUNTIME ${EXECJS_RUNTIME}
diff --git a/docker/dockerfiles/rails.Dockerfile b/docker/dockerfiles/rails.Dockerfile
index 8617ba8ce..810fe8c5f 100644
--- a/docker/dockerfiles/rails.Dockerfile
+++ b/docker/dockerfiles/rails.Dockerfile
@@ -1,5 +1,15 @@
 FROM chatwoot:development
 
+# >>> ADICIONE ISTO AQUI <<<
+RUN apk add --no-cache \
+  build-base \
+  linux-headers \
+  libffi-dev
+# >>> FIM <<<
+
+# RUN gem install bundler -v 2.5.11
+
+
 ENV PNPM_HOME="/root/.local/share/pnpm"
 ENV PATH="$PNPM_HOME:$PATH"
 
diff --git a/docker/entrypoints/vite.sh b/docker/entrypoints/vite.sh
index 8af968ff7..6b4ac15ba 100755
--- a/docker/entrypoints/vite.sh
+++ b/docker/entrypoints/vite.sh
@@ -4,6 +4,9 @@ set -x
 rm -rf /app/tmp/pids/server.pid
 rm -rf /app/tmp/cache/*
 
+# install missing gems
+bundle check || bundle install
+
 pnpm store prune
 pnpm install --force
 
diff --git a/vite.config.ts b/vite.config.ts
index e06b0f1fb..7e41c497d 100644
--- a/vite.config.ts
+++ b/vite.config.ts
@@ -44,6 +44,17 @@ if (isLibraryMode) {
 
 export default defineConfig({
   plugins: plugins,
+  server:{
+    host: process.env.VITE_RUBY_HOST || 'localhost',
+    allowedHosts:['localhost', '127.0.0.1', 'vite'],
+    hmr: {
+      host: 'localhost',
+      clientPort: 3036,
+    },
+    watch: {
+      usePolling: true,
+    },
+  },
   build: {
     rollupOptions: {
       output: {
