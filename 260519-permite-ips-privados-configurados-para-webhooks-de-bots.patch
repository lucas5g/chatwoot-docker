diff --git a/lib/webhooks/trigger.rb b/lib/webhooks/trigger.rb
index 92c55982f..0596e27cc 100644
--- a/lib/webhooks/trigger.rb
+++ b/lib/webhooks/trigger.rb
@@ -60,21 +60,7 @@ class Webhooks::Trigger
   end
 
   def allowed_private_ip_ranges
-    common_allowed_private_ip_ranges.presence || webhook_type_allowed_private_ip_ranges
-  end
-
-  def common_allowed_private_ip_ranges
-    private_ip_ranges_from_env('ALLOWED_PRIVATE_IP_RANGES', 'ALLOWED_PRIVATE_IP_RANGE')
-  end
-
-  def webhook_type_allowed_private_ip_ranges
-    return private_ip_ranges_from_env('AGENT_BOT_ALLOWED_PRIVATE_IP_RANGES', 'AGENT_BOT_ALLOWED_PRIVATE_IP_RANGE') if @webhook_type == :agent_bot_webhook
-
-    private_ip_ranges_from_env('WEBHOOK_ALLOWED_PRIVATE_IP_RANGES', 'WEBHOOK_ALLOWED_PRIVATE_IP_RANGE')
-  end
-
-  def private_ip_ranges_from_env(plural_key, singular_key)
-    (ENV[plural_key].presence || ENV[singular_key].to_s).split(',').map(&:strip)
+    ENV['ALLOWED_PRIVATE_IPS'].to_s.split(',').map(&:strip)
   end
 
   def request_headers(body)
diff --git a/spec/lib/webhooks/trigger_spec.rb b/spec/lib/webhooks/trigger_spec.rb
index 571ec0a53..9f2939489 100644
--- a/spec/lib/webhooks/trigger_spec.rb
+++ b/spec/lib/webhooks/trigger_spec.rb
@@ -167,7 +167,7 @@ describe Webhooks::Trigger do
       it 'passes configured private IP ranges to agent bot webhooks' do
         payload = { event: 'message_created', id: pending_message.id }
 
-        with_modified_env ALLOWED_PRIVATE_IP_RANGES: '10.230.32.9/32' do
+        with_modified_env ALLOWED_PRIVATE_IPS: '10.230.32.9/32' do
           expect(SafeFetch).to receive(:fetch).with(
             url,
             method: :post,
@@ -183,43 +183,6 @@ describe Webhooks::Trigger do
         end
       end
 
-      it 'supports agent bot specific private IP ranges' do
-        payload = { event: 'message_created', id: pending_message.id }
-
-        with_modified_env AGENT_BOT_ALLOWED_PRIVATE_IP_RANGES: '10.230.32.9/32' do
-          expect(SafeFetch).to receive(:fetch).with(
-            url,
-            method: :post,
-            body: payload.to_json,
-            headers: base_headers,
-            open_timeout: webhook_timeout,
-            read_timeout: webhook_timeout,
-            validate_content_type: false,
-            allow_private_ip_ranges: ['10.230.32.9/32']
-          ).and_yield(fetch_result)
-
-          trigger.execute(url, payload, webhook_type)
-        end
-      end
-
-      it 'supports the legacy singular private IP range setting' do
-        payload = { event: 'message_created', id: pending_message.id }
-
-        with_modified_env AGENT_BOT_ALLOWED_PRIVATE_IP_RANGE: '10.230.32.9' do
-          expect(SafeFetch).to receive(:fetch).with(
-            url,
-            method: :post,
-            body: payload.to_json,
-            headers: base_headers,
-            open_timeout: webhook_timeout,
-            read_timeout: webhook_timeout,
-            validate_content_type: false,
-            allow_private_ip_ranges: ['10.230.32.9']
-          ).and_yield(fetch_result)
-
-          trigger.execute(url, payload, webhook_type)
-        end
-      end
     end
 
     it 'handles 500 without raising for non-agent webhooks' do
@@ -234,7 +197,7 @@ describe Webhooks::Trigger do
     it 'passes configured private IP ranges to regular webhooks' do
       payload = { event: 'message_created', conversation: { id: conversation.id }, id: message.id }
 
-      with_modified_env ALLOWED_PRIVATE_IP_RANGES: '10.230.32.58' do
+      with_modified_env ALLOWED_PRIVATE_IPS: '10.230.32.58' do
         expect(SafeFetch).to receive(:fetch).with(
           url,
           method: :post,
